name: map_mag

hidden_dim: 256
dropout: 0.2
norm: layernorm

# Structure-aware diffusion path.
num_hops: 2
# Keep NeighborLoader sampling depth aligned with MAP-MAG diffusion depth.
num_layers: ${model.num_hops}
restart: 0.15
diffusion_add_self_loops: true

# Semantic prototype path.
num_prototypes: 16
lambda_proto: 0.1

# Router balance regularization.
reliability_min: 0.1
router_temperature: 2.0
lambda_gate: 0.01
use_gate_loss: true
gate_warmup_epochs: 0

# Frequency gate range. This softly bounds gamma away from exact 0/1.
gamma_min: 0.05
gamma_max: 0.95
gamma_temperature: 3.0
gamma_warmup_epochs: 0
fixed_gamma: null

# Ablation switches.
use_self_path: true
use_structure_path: true
use_prototype_path: true
use_preference_router: true
structure_low_pass_only: false
self_only: false

# MAP-MAG can train with sampled subgraphs. Set true for exact full-graph
# training on small graphs if desired.
full_graph_training: true
